# .codex/SESSION.md

## 2026-04-17

- Separated Windows content from the old mixed `supervisor-setting` repository into this repository.
- Moved Linux reference material to `C:\Users\Joon\Projects\supervisor-linux`.
- Removed the old nested `supervisor-setting` folder after verification.
- Initialized Git in `C:\Users\Joon\Projects\supervisor-windows`.
- Added tracked `AGENT.md` plus local handoff files and gitignore rules for those local files.

## 2026-04-23

- User created the GitHub repository for `supervisor-windows`.
- User mirrored the old `supervisor-setting` repository into the new GitHub repository.
- User fetched from the new remote, ran a mixed reset against `origin/main`, and kept the resulting changes in the index/worktree.
- User created a follow-up local commit for the split repository.
- Current remote observed locally: `origin = https://github.com/SinclairQuantumLab/supervisor-windows.git`.
- Local handoff/context files were moved under `.codex/` to keep the repository root clean.
- Changed the `.codex/` policy so durable project context is tracked and only `.local.md` files are ignored.
